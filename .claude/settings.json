{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && uv run ruff check --fix --quiet 2>/dev/null; uv run ruff format --quiet 2>/dev/null; exit 0",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
